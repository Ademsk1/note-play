import { RecordingContext } from '../hooks/useRecording';

const RecordingProvider = ({ children }) => {
  const [];

  return <RecordingContext value={null}>{children}</RecordingContext>;
};

export default RecordingProvider;
